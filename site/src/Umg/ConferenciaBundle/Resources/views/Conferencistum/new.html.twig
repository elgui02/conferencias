{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nuevo conferencista</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('conferencista') }}">
            Regresar a la lista
        </a>
    </li>
</ul>
{% endblock %}
