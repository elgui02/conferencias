{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AlumnoEvento</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Alumno_id</th>
                <td>{{ entity.Alumnoid }}</td>
            </tr>
            <tr>
                <th>Evento_id</th>
                <td>{{ entity.Eventoid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('alumnoevento') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('alumnoevento_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
