{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar conferencista</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('conferencista') }}">
            Regresar a la lista
        </a>
    </li>
</ul>
{% endblock %}
