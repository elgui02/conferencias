{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evento edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('evento') }}">
            Regresar a la lista
        </a>
    </li>
</ul>
{% endblock %}
