{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Editar alumno</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('alumno') }}">
            Regresar a la lista
        </a>
    </li>
</ul>
{% endblock %}
