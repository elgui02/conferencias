{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AlumnoPago list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Boleta</th>
                <th>Alumno_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('alumnopago_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Boleta }}</td>
                <td>{{ entity.Alumnoid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('alumnopago_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('alumnopago_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('alumnopago_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
